#include <iostream>
#include "src/Point.h"
#include "src/Gradient.h"
#include "src/CommonRandom.h"

int main() {
//    Rosenbrock f = Rosenbrock();
//    auto a = Gradient<2>(f, Point<2>(std::array<double, 2>({0., 0.})));
    std::cout << Point<2>(std::array<double, 2>({0., 0.}));
    return 0;
}